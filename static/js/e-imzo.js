/**
 * Blank wrapper or standard E-IMZO helper
 * Used alongside e-imzo-client.js to match the typical implementation script structure
 */
if (typeof window.EIMZO !== "undefined") {
    // defined
} else {
    window.EIMZO = {};
}
